# NOTA: bumps de seguridad — ver `docs/SECURITY_UPDATES_2026-05.md`.
# Cuando upgradés, verificar que tests pasan + smoke test de chat,
# auth y stripe webhook (los 3 endpoints que más tocan deps actualizadas).
# fastapi 0.121.0 es el primer release que widena el pin de starlette
# a <0.50 — necesario para mantener starlette==0.49.1 con sus CVE fixes
# (PYSEC-2026-161, CVE-2025-54121, CVE-2025-62727).
fastapi==0.121.0
# starlette es transitiva de fastapi. fastapi 0.121.0 acepta
# starlette>=0.40.0,<0.50.0, así que 0.49.1 queda dentro del rango.
starlette==0.49.1
uvicorn[standard]==0.34.2
pydantic==2.11.3
pydantic-settings==2.9.1
pydantic[email]==2.11.3
python-dotenv==1.2.2          # CVE-2026-28684 fixed
httpx==0.28.1
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
alembic==1.18.4
greenlet==3.4.0
slowapi==0.1.9
PyJWT==2.12.0                 # PYSEC-2026-120 + PYSEC-2025-183 fixed
python-multipart==0.0.27      # CVE-2026-24486 + 40347 + 42561 fixed
anthropic==0.40.0
bcrypt==4.3.0
stripe==11.4.1
# ── Agent / SOL ─────────────────────────────────────────────────────
# PDF generation for export tools (resúmenes de proyecto, pagos, etc.)
reportlab==4.2.5
# DOCX generation (resúmenes editables)
python-docx==1.1.2
# ── Monitoring ──────────────────────────────────────────────────────
sentry-sdk[fastapi]==2.18.0
