# HANDOFF 2026-06-22 — estado y continuación

> **Fuente de verdad del estado actual.** Las reglas fijas de cómo trabajar están
> en `CLAUDE.md` (raíz del repo) — leelo primero. Handoff anterior:
> `docs/HANDOFF_2026-06-03.md` (histórico).

## TL;DR
Modelo **pago-only** terminado y en `main`. **Backend en Railway: LIVE y
verificado.** **Mercado Pago** está completo en código pero **inerte** hasta que
Agustín cargue credenciales. ⚠️ **El frontend nuevo NO está publicado**: Netlify
es de la cuenta de Agus y no auto-deploya, y **nosotros no tenemos permisos**.
Lo que falta para lanzar son **acciones de Agustín** (Netlify + env vars + MP),
todas listadas en `docs/PARA_AGUSTIN.md`.

- `main` snapshot al escribir esto: `7c7883c` (se mueve; Agus pushea seguido).
- Branch de trabajo: `merge/launch-mvp-into-main` (= main).
- Branch `fix/capa2-calculadora`: Bug2 del prompt de Transferencia, para review de Agus.

## Hecho de nuestro lado (todo en `main`)
- **Pago-only**: planes trial(7d)/pro/inactive. `core/plan_gate.py`
  (`has_access` / `require_access`) + gate en todos los routers de producto.
- **Mercado Pago** — `services/mercadopago_service.py`, env-gated (inerte sin
  `MP_ACCESS_TOKEN`+`MP_PLAN_ID`):
  - Checkout (preapproval): `POST /api/billing/checkout` despacha a MP si está
    configurado, sino cae a Stripe legacy.
  - Webhook con firma HMAC: `POST /api/billing/mp/webhook`.
  - **Botón de baja** (online, Ley 24.240): `POST /api/billing/mp/cancel` + UI
    en `frontend/account.html`.
  - Config pública: `GET /api/billing/mp/config` (`{enabled, public_key}`).
  - Settings `MP_*` en `config/settings.py` y `backend/.env.example`.
- **Onboarding tarjeta-upfront**: con MP activo, el registro arranca `inactive` y
  el usuario debe suscribir; sin MP, trial directo (comportamiento actual). Front:
  paywall proactivo en `app.html`.
- **Bypass de paywall para admins**: emails en `ADMIN_EMAILS` tienen acceso
  siempre (los fundadores no quedan afuera de su propia app cuando vence el trial).
- **Frontend pago-only**: copy sin "gratis"/freemium, precio $69.900, **`app.css`
  extraído** de `app.html` (deuda técnica, slice 1), account/pricing coherentes.
- **KB admin**: `frontend/admin.html` + `require_admin` (cierra el agujero de
  escritura al KB).
- **Bug1 de Capa 2** (`_spread` descartaba costo de obra fuera del horizonte →
  inflaba TIR/margen): corregido y en main (`df7f7e7`). Era un bug de cálculo vivo.

## 🔴 BLOQUEADO — acciones de Agustín (no podemos nosotros)
1. **NETLIFY — el frontend no publica.** Sitio en la cuenta de Agus; publica por
   Netlify Drop (manual); los pushes a `main` figuran **"Skipped"**. **No tenemos
   permisos de Netlify.** Todo el frontend nuevo (modelo pago, precio, app.css,
   baja MP — y lo que sumó Agus: botones Compartir, entregables) está en `main`
   esperando. **Fix:** que Agus conecte Continuous Deployment al repo
   (branch `main`, base `frontend`, publish `.`, sin build command) o dispare un
   deploy. Detalle exacto: `docs/PARA_AGUSTIN.md` §0.
2. **`ADMIN_EMAILS` en Railway** — sin esto los fundadores quedan paywalled (el
   trial ya venció) y no se puede gestionar el KB. Es lo más rápido y urgente.
3. **Mercado Pago** — Agus crea el plan ($69.900/mes, trial 7d), configura el
   webhook (`/api/billing/mp/webhook`) y carga `MP_ACCESS_TOKEN`, `MP_PUBLIC_KEY`,
   `MP_WEBHOOK_SECRET`, `MP_PLAN_ID` en Railway. Ahí se enciende solo.
4. Resend (emails), Sentry, UptimeRobot, backups Supabase — ver `PARA_AGUSTIN.md`.
5. **Bug2 Capa 2** (prompt de Transferencia) en branch `fix/capa2-calculadora`
   para que Agus lo revise/mergee (ya está mitigado por su "disparador de tool").

## ✅ Qué puede avanzar el próximo Claude SIN bloqueos
- **Cuando Netlify publique** (Agus avisa): cerrar la verificación frontend en
  prod — `app.css` 200, copy nuevo, y screenshot de `app.html` logueado con la
  cuenta demo (confirma que el CSS extraído aplica bien).
- **Cuando lleguen credenciales MP**: conectar y probar el flujo completo contra
  el **sandbox** de MP (checkout → webhook → acceso → baja). El código ya está;
  puede necesitar ajuste fino del request del preapproval (init_point vs
  card_token) — está comentado en `mercadopago_service.py`.
- **Deuda técnica nuestra**: `app.html` sigue siendo grande (ya se extrajo el
  CSS); se puede seguir modularizando el JS si Mati lo prioriza.
- Lo que Mati priorice del backlog nuestro.

## ⛔ NO tocar (dominio de Agustín)
chat, `services/calculator_tools.py`, el system prompt de
`services/anthropic_service.py`, `services/financial_artifact.py`,
`services/document_service.py`, y carpetas / retrieval / guardado de info del
chat. Bug ahí → branch + review de Agus.

## Setup en una PC nueva
1. `git clone` (o `git pull`); quedás en `merge/launch-mvp-into-main`.
2. Crear `backend/.env` copiando `backend/.env.example` y completando con los
   **secrets reales** (los tenés vos / están en Railway → Variables). No se commitea.
3. venv + `pip install -r backend/requirements.txt -r backend/requirements-dev.txt`.
4. Tests/lint/deploy: ver `CLAUDE.md`.

## Estado de prod (verificado al 2026-06-22)
- **Railway (backend): LIVE.** `/health` 200 · `/api/billing/mp/config` 200
  `{enabled:false}` (MP inerte, OK) · `/api/billing/mp/cancel` y demás protegidos
  → 401 sin token (gates correctos).
- **Netlify (frontend): viejo.** Sirve el frontend pre-cambios (`app.css` 404)
  hasta que Agus publique.

## Punteros
- `docs/PARA_AGUSTIN.md` — checklist de activaciones de Agus (Netlify §0, MP, env vars).
- `docs/MODELO_PAGO.md` — diseño del modelo pago + Mercado Pago.
- `docs/REVISION_CAPA2_2026-06-06.md` — los 2 bugs de Capa 2 (Bug1 ya mergeado).
- `backend/.env.example` — todas las env vars con explicación.
